# Pi SDK Plugins Catalog

180+ npm packages compatible with [@earendil-works/pi-coding-agent](https://github.com/earendil-works/pi),
grouped by function. Source data from npm registry; HTML page is `index.html`.

Served via GitHub Pages.
